# Legacy static routing rules; the Cloudflare Worker owns SSR routing.
#
# Dynamic routes /e/{identifier}, /e/{identifier}/momentos, /events/{identifier}/upload,
# /rsvp/{identifier}, and /evento are handled by SSR (Cloudflare Pages Functions).
# No rewrite rules needed — the _worker.js handles them directly.
#
# Only add rewrites here for static fallback routes that are NOT handled by SSR.
